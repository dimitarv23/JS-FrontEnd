function getSubstring(input, startIndex, endIndex) {
    let substring = input.substring(startIndex, endIndex + 1);

    console.log(substring);
}

getSubstring("ASentence", 1, 8);
getSubstring("SkipWord", 4, 7);
